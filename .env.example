# =============================================================================
# APEX Engine - Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
#
# The application will read these values automatically on startup.
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED FOR AUDIO GENERATION
# -----------------------------------------------------------------------------
# Sonauto API key via fal.ai
# Get yours at: https://fal.ai/dashboard/keys
# Pricing: $0.075 per generation (approximately)
FAL_KEY=your_fal_api_key_here

# -----------------------------------------------------------------------------
# REQUIRED FOR AI LYRIC GENERATION
# -----------------------------------------------------------------------------
# OpenAI API key for GPT-4o
# Get yours at: https://platform.openai.com/api-keys
# Note: Standard OpenAI pricing applies for GPT-4o usage
OPENAI_API_KEY=your_openai_api_key_here

# -----------------------------------------------------------------------------
# OPTIONAL CONFIGURATION
# -----------------------------------------------------------------------------

# Flask secret key for session management
# Generate a random key: python -c "import secrets; print(secrets.token_hex(32))"
# If not set, a default development key is used (NOT SECURE FOR PRODUCTION)
SECRET_KEY=your_secret_key_here

# Flask debug mode (set to 0 for production)
FLASK_DEBUG=1

# Server binding configuration
HOST=0.0.0.0
PORT=5000

# -----------------------------------------------------------------------------
# SIMULATION MODE
# -----------------------------------------------------------------------------
# If API keys are not configured, the application runs in "Simulation Mode":
# - Lyrics are generated using template-based simulation
# - Audio generation is disabled but UI remains functional
# - Useful for development and testing without API costs
#
# To enable simulation mode intentionally, comment out or remove the API keys above.
# -----------------------------------------------------------------------------
